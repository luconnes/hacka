# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class SessaoActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Sessao]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Sessao.prisma().query_raw(
            'SELECT * FROM Sessao WHERE id = $1',
            1103527590,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Sessao
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Sessao.prisma().query_first(
            'SELECT * FROM Sessao WHERE estado = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.SessaoCreateInput,
        include: Optional[types.SessaoInclude] = None
    ) -> _PrismaModelT:
        """Create a new Sessao record.

        Parameters
        ----------
        data
            Sessao record data
        include
            Specifies which relations should be loaded on the returned Sessao model

        Returns
        -------
        prisma.models.Sessao
            The created Sessao record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Sessao record from just the required fields
        sessao = await Sessao.prisma().create(
            data={
                # data to create a Sessao record
                'estado': 'ggciceaie',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.SessaoCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Sessao records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Sessao record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Sessao.prisma().create_many(
            data=[
                {
                    # data to create a Sessao record
                    'estado': 'bbehjachib',
                },
                {
                    # data to create a Sessao record
                    'estado': 'cadfabfehe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.SessaoWhereUniqueInput,
        include: Optional[types.SessaoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Sessao record.

        Parameters
        ----------
        where
            Sessao filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Sessao model

        Returns
        -------
        prisma.models.Sessao
            The deleted Sessao record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sessao = await Sessao.prisma().delete(
            where={
                'id': 368800899,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.SessaoWhereUniqueInput,
        include: Optional[types.SessaoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Sessao record.

        Parameters
        ----------
        where
            Sessao filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Sessao model

        Returns
        -------
        prisma.models.Sessao
            The found Sessao record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sessao = await Sessao.prisma().find_unique(
            where={
                'id': 1508029952,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.SessaoWhereUniqueInput,
        include: Optional[types.SessaoInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Sessao record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Sessao filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Sessao model

        Returns
        -------
        prisma.models.Sessao
            The found Sessao record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sessao = await Sessao.prisma().find_unique_or_raise(
            where={
                'id': 486256185,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessaoWhereInput] = None,
        cursor: Optional[types.SessaoWhereUniqueInput] = None,
        include: Optional[types.SessaoInclude] = None,
        order: Optional[Union[types.SessaoOrderByInput, List[types.SessaoOrderByInput]]] = None,
        distinct: Optional[List[types.SessaoScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Sessao records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Sessao records returned
        skip
            Ignore the first N results
        where
            Sessao filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Sessao model
        order
            Order the returned Sessao records by any field
        distinct
            Filter Sessao records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Sessao]
            The list of all Sessao records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Sessao records
        sessaos = await Sessao.prisma().find_many(take=10)

        # find the first 5 Sessao records ordered by the criadoEm field
        sessaos = await Sessao.prisma().find_many(
            take=5,
            order={
                'criadoEm': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SessaoWhereInput] = None,
        cursor: Optional[types.SessaoWhereUniqueInput] = None,
        include: Optional[types.SessaoInclude] = None,
        order: Optional[Union[types.SessaoOrderByInput, List[types.SessaoOrderByInput]]] = None,
        distinct: Optional[List[types.SessaoScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Sessao record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Sessao filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Sessao model
        order
            Order the returned Sessao records by any field
        distinct
            Filter Sessao records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Sessao
            The first Sessao record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Sessao record ordered by the id field
        sessao = await Sessao.prisma().find_first(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SessaoWhereInput] = None,
        cursor: Optional[types.SessaoWhereUniqueInput] = None,
        include: Optional[types.SessaoInclude] = None,
        order: Optional[Union[types.SessaoOrderByInput, List[types.SessaoOrderByInput]]] = None,
        distinct: Optional[List[types.SessaoScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Sessao record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Sessao filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Sessao model
        order
            Order the returned Sessao records by any field
        distinct
            Filter Sessao records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Sessao
            The first Sessao record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Sessao record ordered by the estado field
        sessao = await Sessao.prisma().find_first_or_raise(
            skip=1,
            order={
                'estado': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.SessaoUpdateInput,
        where: types.SessaoWhereUniqueInput,
        include: Optional[types.SessaoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Sessao record.

        Parameters
        ----------
        data
            Sessao record data specifying what to update
        where
            Sessao filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Sessao model

        Returns
        -------
        prisma.models.Sessao
            The updated Sessao record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        sessao = await Sessao.prisma().update(
            where={
                'id': 1062517886,
            },
            data={
                # data to update the Sessao record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.SessaoWhereUniqueInput,
        data: types.SessaoUpsertInput,
        include: Optional[types.SessaoInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Sessao filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Sessao model

        Returns
        -------
        prisma.models.Sessao
            The created or updated Sessao record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sessao = await Sessao.prisma().upsert(
            where={
                'id': 267834847,
            },
            data={
                'create': {
                    'id': 267834847,
                    'estado': 'cadfabfehe',
                },
                'update': {
                    'estado': 'cadfabfehe',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.SessaoUpdateManyMutationInput,
        where: types.SessaoWhereInput,
    ) -> int:
        """Update multiple Sessao records

        Parameters
        ----------
        data
            Sessao data to update the selected Sessao records to
        where
            Filter to select the Sessao records to update

        Returns
        -------
        int
            The total number of Sessao records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Sessao records
        total = await Sessao.prisma().update_many(
            data={
                'criadoEm': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessaoWhereInput] = None,
        cursor: Optional[types.SessaoWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Sessao records present in the database

        Parameters
        ----------
        select
            Select the Sessao fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Sessao filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SessaoCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Sessao.prisma().count()

        # results: prisma.types.SessaoCountAggregateOutput
        results = await Sessao.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.SessaoCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessaoWhereInput] = None,
        cursor: Optional[types.SessaoWhereUniqueInput] = None,
    ) -> types.SessaoCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.SessaoCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SessaoWhereInput] = None,
        cursor: Optional[types.SessaoWhereUniqueInput] = None,
    ) -> Union[int, types.SessaoCountAggregateOutput]:
        """Count the number of Sessao records present in the database

        Parameters
        ----------
        select
            Select the Sessao fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Sessao filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SessaoCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Sessao.prisma().count()

        # results: prisma.types.SessaoCountAggregateOutput
        results = await Sessao.prisma().count(
            select={
                '_all': True,
                'estado': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SessaoCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.SessaoWhereInput] = None
    ) -> int:
        """Delete multiple Sessao records.

        Parameters
        ----------
        where
            Optional Sessao filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Sessao records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Sessao records
        total = await Sessao.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.SessaoScalarFieldKeys'],
        *,
        where: Optional['types.SessaoWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SessaoAvgAggregateInput'] = None,
        sum: Optional['types.SessaoSumAggregateInput'] = None,
        min: Optional['types.SessaoMinAggregateInput'] = None,
        max: Optional['types.SessaoMaxAggregateInput'] = None,
        having: Optional['types.SessaoScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SessaoCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SessaoScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SessaoScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SessaoGroupByOutput']:
        """Group Sessao records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Sessao fields to group records by
        where
            Sessao filter to select records
        take
            Limit the maximum number of Sessao records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SessaoGroupByOutput]
            A list of dictionaries representing the Sessao record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Sessao records by criadoEm values
        # and count how many records are in each group
        results = await Sessao.prisma().group_by(
            ['criadoEm'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AmplitudeActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Amplitude]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Amplitude.prisma().query_raw(
            'SELECT * FROM Amplitude WHERE id = $1',
            180171308,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Amplitude
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Amplitude.prisma().query_first(
            'SELECT * FROM Amplitude WHERE sessaoId = $1',
            836760821,
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AmplitudeCreateInput,
        include: Optional[types.AmplitudeInclude] = None
    ) -> _PrismaModelT:
        """Create a new Amplitude record.

        Parameters
        ----------
        data
            Amplitude record data
        include
            Specifies which relations should be loaded on the returned Amplitude model

        Returns
        -------
        prisma.models.Amplitude
            The created Amplitude record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Amplitude record from just the required fields
        amplitude = await Amplitude.prisma().create(
            data={
                # data to create a Amplitude record
                'sessaoId': 595337866,
                'articulacao': 'hjaecfifb',
                'anguloMaximo': 2111915288.114975,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AmplitudeCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Amplitude records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Amplitude record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Amplitude.prisma().create_many(
            data=[
                {
                    # data to create a Amplitude record
                    'sessaoId': 1644289366,
                    'articulacao': 'bdiicjafbj',
                    'anguloMaximo': 1647418052.167554,
                },
                {
                    # data to create a Amplitude record
                    'sessaoId': 1767274722,
                    'articulacao': 'dcgchcbbf',
                    'anguloMaximo': 1343201072.67578,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AmplitudeWhereUniqueInput,
        include: Optional[types.AmplitudeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Amplitude record.

        Parameters
        ----------
        where
            Amplitude filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Amplitude model

        Returns
        -------
        prisma.models.Amplitude
            The deleted Amplitude record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        amplitude = await Amplitude.prisma().delete(
            where={
                'id': 744964398,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AmplitudeWhereUniqueInput,
        include: Optional[types.AmplitudeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Amplitude record.

        Parameters
        ----------
        where
            Amplitude filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Amplitude model

        Returns
        -------
        prisma.models.Amplitude
            The found Amplitude record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        amplitude = await Amplitude.prisma().find_unique(
            where={
                'id': 1969681615,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AmplitudeWhereUniqueInput,
        include: Optional[types.AmplitudeInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Amplitude record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Amplitude filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Amplitude model

        Returns
        -------
        prisma.models.Amplitude
            The found Amplitude record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        amplitude = await Amplitude.prisma().find_unique_or_raise(
            where={
                'id': 1116175964,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AmplitudeWhereInput] = None,
        cursor: Optional[types.AmplitudeWhereUniqueInput] = None,
        include: Optional[types.AmplitudeInclude] = None,
        order: Optional[Union[types.AmplitudeOrderByInput, List[types.AmplitudeOrderByInput]]] = None,
        distinct: Optional[List[types.AmplitudeScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Amplitude records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Amplitude records returned
        skip
            Ignore the first N results
        where
            Amplitude filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Amplitude model
        order
            Order the returned Amplitude records by any field
        distinct
            Filter Amplitude records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Amplitude]
            The list of all Amplitude records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Amplitude records
        amplitudes = await Amplitude.prisma().find_many(take=10)

        # find the first 5 Amplitude records ordered by the articulacao field
        amplitudes = await Amplitude.prisma().find_many(
            take=5,
            order={
                'articulacao': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AmplitudeWhereInput] = None,
        cursor: Optional[types.AmplitudeWhereUniqueInput] = None,
        include: Optional[types.AmplitudeInclude] = None,
        order: Optional[Union[types.AmplitudeOrderByInput, List[types.AmplitudeOrderByInput]]] = None,
        distinct: Optional[List[types.AmplitudeScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Amplitude record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Amplitude filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Amplitude model
        order
            Order the returned Amplitude records by any field
        distinct
            Filter Amplitude records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Amplitude
            The first Amplitude record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Amplitude record ordered by the anguloMaximo field
        amplitude = await Amplitude.prisma().find_first(
            skip=1,
            order={
                'anguloMaximo': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AmplitudeWhereInput] = None,
        cursor: Optional[types.AmplitudeWhereUniqueInput] = None,
        include: Optional[types.AmplitudeInclude] = None,
        order: Optional[Union[types.AmplitudeOrderByInput, List[types.AmplitudeOrderByInput]]] = None,
        distinct: Optional[List[types.AmplitudeScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Amplitude record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Amplitude filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Amplitude model
        order
            Order the returned Amplitude records by any field
        distinct
            Filter Amplitude records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Amplitude
            The first Amplitude record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Amplitude record ordered by the id field
        amplitude = await Amplitude.prisma().find_first_or_raise(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AmplitudeUpdateInput,
        where: types.AmplitudeWhereUniqueInput,
        include: Optional[types.AmplitudeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Amplitude record.

        Parameters
        ----------
        data
            Amplitude record data specifying what to update
        where
            Amplitude filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Amplitude model

        Returns
        -------
        prisma.models.Amplitude
            The updated Amplitude record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        amplitude = await Amplitude.prisma().update(
            where={
                'id': 861472101,
            },
            data={
                # data to update the Amplitude record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AmplitudeWhereUniqueInput,
        data: types.AmplitudeUpsertInput,
        include: Optional[types.AmplitudeInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Amplitude filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Amplitude model

        Returns
        -------
        prisma.models.Amplitude
            The created or updated Amplitude record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        amplitude = await Amplitude.prisma().upsert(
            where={
                'id': 1303003706,
            },
            data={
                'create': {
                    'id': 1303003706,
                    'sessaoId': 1767274722,
                    'articulacao': 'dcgchcbbf',
                    'anguloMaximo': 1343201072.67578,
                },
                'update': {
                    'sessaoId': 1767274722,
                    'articulacao': 'dcgchcbbf',
                    'anguloMaximo': 1343201072.67578,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AmplitudeUpdateManyMutationInput,
        where: types.AmplitudeWhereInput,
    ) -> int:
        """Update multiple Amplitude records

        Parameters
        ----------
        data
            Amplitude data to update the selected Amplitude records to
        where
            Filter to select the Amplitude records to update

        Returns
        -------
        int
            The total number of Amplitude records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Amplitude records
        total = await Amplitude.prisma().update_many(
            data={
                'sessaoId': 1686638315
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AmplitudeWhereInput] = None,
        cursor: Optional[types.AmplitudeWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Amplitude records present in the database

        Parameters
        ----------
        select
            Select the Amplitude fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Amplitude filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AmplitudeCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Amplitude.prisma().count()

        # results: prisma.types.AmplitudeCountAggregateOutput
        results = await Amplitude.prisma().count(
            select={
                '_all': True,
                'articulacao': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AmplitudeCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AmplitudeWhereInput] = None,
        cursor: Optional[types.AmplitudeWhereUniqueInput] = None,
    ) -> types.AmplitudeCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AmplitudeCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AmplitudeWhereInput] = None,
        cursor: Optional[types.AmplitudeWhereUniqueInput] = None,
    ) -> Union[int, types.AmplitudeCountAggregateOutput]:
        """Count the number of Amplitude records present in the database

        Parameters
        ----------
        select
            Select the Amplitude fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Amplitude filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AmplitudeCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Amplitude.prisma().count()

        # results: prisma.types.AmplitudeCountAggregateOutput
        results = await Amplitude.prisma().count(
            select={
                '_all': True,
                'anguloMaximo': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AmplitudeCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AmplitudeWhereInput] = None
    ) -> int:
        """Delete multiple Amplitude records.

        Parameters
        ----------
        where
            Optional Amplitude filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Amplitude records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Amplitude records
        total = await Amplitude.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AmplitudeScalarFieldKeys'],
        *,
        where: Optional['types.AmplitudeWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AmplitudeAvgAggregateInput'] = None,
        sum: Optional['types.AmplitudeSumAggregateInput'] = None,
        min: Optional['types.AmplitudeMinAggregateInput'] = None,
        max: Optional['types.AmplitudeMaxAggregateInput'] = None,
        having: Optional['types.AmplitudeScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AmplitudeCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AmplitudeScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AmplitudeScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AmplitudeGroupByOutput']:
        """Group Amplitude records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Amplitude fields to group records by
        where
            Amplitude filter to select records
        take
            Limit the maximum number of Amplitude records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AmplitudeGroupByOutput]
            A list of dictionaries representing the Amplitude record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Amplitude records by id values
        # and count how many records are in each group
        results = await Amplitude.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models